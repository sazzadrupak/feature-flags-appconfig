const __locales = ["ja"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `"${params?.keyword}" キーワード検証に合格する必要があります`
const comparisonLimit = (params) => `${params?.comparison} ${params?.limit}でなければいけない`
export const items = (params) => `は${__formatVariable(params?.limit)}以上あってはいけない`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `追加してはいけない`
export const anyOf = `"anyOf"のスキーマとマッチしなくてはいけない`
export const _const = `定数に等しくなければなりません`
export const contains = `有効なアイテムが含まれている必要があります`
export const dependencies = (params) => `${__formatVariable(params?.property)}がある場合、は${__formatVariable(params?.deps)}をつけなければいけない`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `事前に定義された値のいずれかに等しくなければいけない`
export const falseSchema = `ブールスキーマが偽です`
export const format = (params) => `"${__formatVariable(params?.format)}"形式に揃えなければいけない`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `"${__formatVariable(params?.failingKeyword)}" スキーマと一致する必要があります`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `は${__formatVariable(params?.limit)}個以上であってはいけない`
export const maxLength = (params) => `は${__formatVariable(params?.limit)}文字以上であってはいけない`
export const maxProperties = (params) => `は${__formatVariable(params?.limit)}個以上のプロパティを有してはいけない`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `は${__formatVariable(params?.limit)}個以下であってはいけない`
export const minLength = (params) => `は${__formatVariable(params?.limit)}文字以下であってはいけない`
export const minProperties = (params) => `は${__formatVariable(params?.limit)}個以下のプロパティを有してはいけない`
export const multipleOf = (params) => `${__formatVariable(params?.multipleOf)}の倍数でなければいけない`
export const not = `"not"のスキーマに従って有効としてはいけない`
export const oneOf = `"oneOf"のスキーマと完全に一致しなくてはいけない`
export const pattern = (params) => `"${__formatVariable(params?.pattern)}"のパターンと一致しなければいけない`
export const patternRequired = (params) => `プロパティ マッチング パターン "${__formatVariable(params?.missingPattern)}" が必要です`
export const propertyNames = `プロパティ名が無効です`
export const required = (params) => `必要なプロパティ${__formatVariable(params?.missingProperty)}がなければいけない`
export const type = (params) => `${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}でなければいけない`
export const unevaluatedItems = (params) => `は${__formatVariable(params?.len)}個以上であってはいけない`
export const unevaluatedProperties = `未評価のプロパティを含めることはできません`
export const uniqueItems = (params) => `重複するアイテムがあってはいけない（${__formatVariable(params?.j)}と${__formatVariable(params?.i)}は同じである）`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `タグ "${__formatVariable(params?.tag)}" は文字列でなければなりません`,
      'mapping': `タグ "${__formatVariable(params?.tag)}" の値はマッピング内にある必要があります`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `追加してはいけない"`,
      'missing': `必要なプロパティ${__formatVariable(params?.missingProperty)}がなければいけない`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `"union"のスキーマとマッチしなくてはいけない`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
