const __locales = ["es"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `debe pasar la validación de palabra clave "${params?.keyword}"`
const comparisonLimit = (params) => `debe ser ${params?.comparison} ${params?.limit}`
export const items = (params) => `no debe tener más de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `elemento`
    },
    `elementos`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `no debe tener propiedades adicionales`
export const anyOf = `debe coincidir con algún esquema en "anyOf"`
export const _const = `debe ser igual a la constante`
export const contains = `debe contener un elemento válido`
export const dependencies = (params) => `debe contener ${__select(
    params?.depsCount,
    {
      'one': `la`
    },
    `las`
  )} ${__select(
    params?.depsCount,
    {
      'one': `propiedad`
    },
    `propiedades`
  )} ${__formatVariable(params?.deps)} cuando la propiedad ${__formatVariable(params?.property)} se encuentra presente`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `deber ser igual a uno de los valores predefinidos`
export const falseSchema = `el esquema és falso`
export const format = (params) => `debe coincidir con el formato "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `debe corresponderse con el esquema "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `no debe contener más de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `elemento`
    },
    `elementos`
  )}`
export const maxLength = (params) => `no debe contener más de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `caracter`
    },
    `caracteres`
  )}`
export const maxProperties = (params) => `no debe contener más de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `propiedad`
    },
    `propiedades`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `no debe contener menos de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `elemento`
    },
    `elementos`
  )}`
export const minLength = (params) => `no debe contener menos de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `caracter`
    },
    `caracteres`
  )}`
export const minProperties = (params) => `no debe contener menos de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `propiedad`
    },
    `propiedades`
  )}`
export const multipleOf = (params) => `debe ser múltiplo de ${__formatVariable(params?.multipleOf)}`
export const not = `no debe ser válido según el esquema en "not"`
export const oneOf = `debe coincidir con un solo esquema en "oneOf"`
export const pattern = (params) => `debe coincidir con el patron "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `la propiedad debe coincidir con el patrón "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `la propiedad no és válida`
export const required = (params) => `debe tener la propiedad requerida ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `debe ser ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `no debe contener más de ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `elemento`
    },
    `elementos`
  )}`
export const unevaluatedProperties = `no debe tener propiedades no evaluadas`
export const uniqueItems = (params) => `no debe contener elementos duplicados, (los elementos ## ${__formatVariable(params?.j)} y ${__formatVariable(params?.i)} son idénticos)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `la etiqueta "${__formatVariable(params?.tag)}" debe ser string`,
      'mapping': `el valor de la etiqueta "${__formatVariable(params?.tag)}" debe estar en la asignación`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `no debe tener propiedades adicionales`,
      'missing': `debe tener la propiedad requerida ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `debe coincidir con algún esquema en "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
