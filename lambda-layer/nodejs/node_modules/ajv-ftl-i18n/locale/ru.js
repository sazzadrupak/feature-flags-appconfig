const __locales = ["ru"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `'должно соответствовать правилу "${params?.keyword}"`
const comparisonLimit = (params) => `должно быть ${params?.comparison} ${params?.limit}`
export const items = (params) => `должно иметь не более, чем ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `элемент`,
      'few': `элементa`
    },
    `элементов`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `не должно иметь дополнительных полей`
export const anyOf = `должно соответствовать одной их схем в "anyOf"`
export const _const = `должно быть равно заданному значению`
export const contains = `должно содержать значение соответствующее схеме`
export const dependencies = (params) => `должно иметь ${__select(
    params?.depsCount,
    {
      'one': `поле`
    },
    `поля`
  )} ${__formatVariable(params?.deps)}, когда присутствует поле ${__formatVariable(params?.property)}`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `должно быть равно одному из разрешенных значений`
export const falseSchema = `схема равна false`
export const format = (params) => `должно соответствовать формату "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `должно соответствовать схемe "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `должно иметь не более, чем ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `элемент`,
      'few': `элементa`
    },
    `элементов`
  )}`
export const maxLength = (params) => `должно быть не длиннее, чем ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `символ`,
      'few': `символa`
    },
    `символов`
  )}`
export const maxProperties = (params) => `должно иметь не более, чем ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `поле`,
      'few': `поля`
    },
    `полей`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `должно иметь не менее, чем ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `элемент`,
      'few': `элементa`
    },
    `элементов`
  )}`
export const minLength = (params) => `должно быть не короче, чем ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `символ`,
      'few': `символa`
    },
    `символов`
  )}`
export const minProperties = (params) => `должно иметь не менее, чем ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `поле`,
      'few': `поля`
    },
    `полей`
  )}`
export const multipleOf = (params) => `должно быть кратным ${__formatVariable(params?.multipleOf)}`
export const not = `должно не соответствовать схеме в "not"`
export const oneOf = `должно соответствовать в точности одной схемe в "oneOf"`
export const pattern = (params) => `должно соответствовать образцу "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `должно иметь поле, соответствующее образцу "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `имя поля не соответствует схеме`
export const required = (params) => `должно иметь обязательное поле ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `должно быть ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `должно иметь не более, чем ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `элемент`,
      'few': `элементa`
    },
    `элементов`
  )}`
export const unevaluatedProperties = `не должно иметь непроверенных полей`
export const uniqueItems = (params) => `не должно иметь повторяющихся элементов (элементы ${__formatVariable(params?.j)} и ${__formatVariable(params?.i)} идентичны)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `поле "${__formatVariable(params?.tag)}" должно быть строкой`,
      'mapping': `значение поля "${__formatVariable(params?.tag)}" должно быть ключом одной из схем`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `не должно иметь дополнительных полей`,
      'missing': `должно иметь обязательное поле ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `должно соответствовать одной их схем в "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
