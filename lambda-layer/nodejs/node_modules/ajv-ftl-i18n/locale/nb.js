const __locales = ["nb"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `må samsvare med valideringen for "${params?.keyword}"`
const comparisonLimit = (params) => `må være ${params?.comparison} ${params?.limit}`
export const items = (params) => `kan ikke ha mer enn ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `element`
    },
    `elementer`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `kan ikke ha flere egenskaper`
export const anyOf = `må samsvare med et schema i "anyOf"`
export const _const = `må være lik konstanten`
export const contains = `må inneholde et gyldig element`
export const dependencies = (params) => `må ha ${__select(
    params?.depsCount,
    {
      'one': `egenskapen`
    },
    `egenskapene`
  )} ${__formatVariable(params?.deps)} når egenskapen ${__formatVariable(params?.property)} er angitt`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `må være lik en av de forhåndsdefinerte verdiene`
export const falseSchema = `boolsk schema er usannt`
export const format = (params) => `må stemme overens med formatet "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `må være lik schema "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `kan ikke ha fler enn ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `element`
    },
    `elementer`
  )}`
export const maxLength = (params) => `kan ikke være lengre enn ${__formatVariable(params?.limit)} tegn`
export const maxProperties = (params) => `kan ikke ha mer enn ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `egenskap`
    },
    `egenskaper`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `kan ikke ha færre enn ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `element`
    },
    `elementer`
  )}`
export const minLength = (params) => `kan ikke være kortere enn ${__formatVariable(params?.limit)} tegn`
export const minProperties = (params) => `kan ikke ha mindre enn ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `egenskap`
    },
    `egenskaper`
  )}`
export const multipleOf = (params) => `må være et multiplum av ${__formatVariable(params?.multipleOf)}`
export const not = `kan ikke samsvare med schema i "not"`
export const oneOf = `må samsvare med nøyaktig ett schema i "oneOf"`
export const pattern = (params) => `må samsvare med mønsteret "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `må ha en egenskap som samsvarer med mønsteret "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `egenskapsnavnet er ugyldig`
export const required = (params) => `må ha den påkrevde egenskapen ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `må være ${__select(
    params?.type,
    {
      'number': `et tall`,
      'integer': `et heltall`,
      'string': `en streng`,
      'boolean': `ja eller nei`
    },
    `${__formatVariable(params?.type)}`
  )}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `kan ikke ha fler enn ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `element`
    },
    `elementer`
  )}`
export const unevaluatedProperties = `kan ikke ha uevaluerte attributter`
export const uniqueItems = (params) => `kan ikke ha duplikate elemeneter (elementene ## ${__formatVariable(params?.j)} og ${__formatVariable(params?.i)} er identiske)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `tag "${__formatVariable(params?.tag)}" må være en streng`,
      'mapping': `verdien av tag "${__formatVariable(params?.tag)}" må finnes i mappingen`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `kan ikke ha flere egenskaper`,
      'missing': `må ha den påkrevde egenskapen ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `må samsvare med et schema i "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
