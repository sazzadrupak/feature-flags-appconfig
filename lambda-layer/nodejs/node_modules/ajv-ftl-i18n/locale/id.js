const __locales = ["id"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `harus lulus validasi kata kunci "${params?.keyword}"`
const comparisonLimit = (params) => `harus ${params?.comparison} ${params?.limit}`
export const items = (params) => `tidak boleh memiliki lebih dari ${__formatVariable(params?.limit)} item`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `tidak boleh memiliki properti tambahan`
export const anyOf = `harus cocok dengan beberapa skema pada "anyOf"`
export const _const = `harus sama dengan konstan`
export const contains = `harus berisi item yang valid`
export const dependencies = (params) => `harus memiliki properti ${__formatVariable(params?.deps)} ketika properti ${__formatVariable(params?.property)} hadir`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `harus sama dengan salah satu dari nilai yang telah ditentukan`
export const falseSchema = `skema boolean salah`
export const format = (params) => `harus cocok dengan format "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `harus cocok dengan skema "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `tidak boleh memiliki lebih dari ${__formatVariable(params?.limit)} item`
export const maxLength = (params) => `tidak boleh lebih dari ${__formatVariable(params?.limit)} karakter`
export const maxProperties = (params) => `tidak boleh memiliki lebih dari ${__formatVariable(params?.limit)} properti`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `tidak boleh kurang dari ${__formatVariable(params?.limit)} item`
export const minLength = (params) => `tidak boleh lebih pendek dari ${__formatVariable(params?.limit)} karakter`
export const minProperties = (params) => `tidak boleh kurang dari ${__formatVariable(params?.limit)} properti`
export const multipleOf = (params) => `harus merupakan kelipatan dari ${__formatVariable(params?.multipleOf)}`
export const not = `tidak boleh valid sesuai dengan skema pada "not"`
export const oneOf = `harus sama persis dengan satu skema pada "oneOf"`
export const pattern = (params) => `harus cocok dengan pola "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `harus memiliki pola pencocokan properti "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `nama properti tidak valid`
export const required = (params) => `harus memiliki properti ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `harus berupa ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `tidak boleh memiliki lebih dari ${__formatVariable(params?.len)} item`
export const unevaluatedProperties = `tidak boleh memiliki sifat yang tidak dievaluasi`
export const uniqueItems = (params) => `tidak boleh memiliki item duplikat (item ## ${__formatVariable(params?.j)} dan ${__formatVariable(params?.i)} identik)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `tag "${__formatVariable(params?.tag)}" harus string`,
      'mapping': `nilai tag "${__formatVariable(params?.tag)}" harus dalam pemetaan`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `tidak boleh memiliki properti tambahan`,
      'missing': `harus memiliki properti ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `harus cocok dengan beberapa skema pada "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
