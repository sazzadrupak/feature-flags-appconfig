const __locales = ["cs"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `musí vyhovět "${params?.keyword}" validaci`
const comparisonLimit = (params) => `musí být ${params?.comparison} ${params?.limit}`
export const items = (params) => `nemůže mít víc, než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `prvků`,
      'few': `prvky`
    },
    `prvek`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `nemůže mít další položky`
export const anyOf = `musí vyhovět alespoň jednomu schématu v "anyOf"`
export const _const = `musí být roven konstantě`
export const contains = `musí obsahovat prvek odpovídající schématu`
export const dependencies = (params) => `musí mít ${__select(
    params?.depsCount,
    {
      'one': `položka`,
      'few': `položky`
    },
    `položek`
  )}: ${__formatVariable(params?.deps)}, pokud obsahuje ${__formatVariable(params?.property)}`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `musí být rovno jedné hodnotě z výčtu`
export const falseSchema = `schéma je false`
export const format = (params) => `musí být ve formátu "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `musí vyhovět "${__formatVariable(params?.failingKeyword)}" schématu`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `nesmí obsahovat víc než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `prvků`,
      'few': `prvky`
    },
    `prvek`
  )}`
export const maxLength = (params) => `nesmí být delší než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `znaků`,
      'few': `znaky`
    },
    `znaek`
  )}`
export const maxProperties = (params) => `nesmí mít víc než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `položka`,
      'few': `položky`
    },
    `položek`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `nesmí obsahovat méně než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `prvků`,
      'few': `prvky`
    },
    `prvek`
  )}`
export const minLength = (params) => `nesmí být kratší než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `znaků`,
      'few': `znaky`
    },
    `znaek`
  )}`
export const minProperties = (params) => `nesmí mít méně než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `položka`,
      'few': `položky`
    },
    `položek`
  )}`
export const multipleOf = (params) => `musí být násobkem ${__formatVariable(params?.multipleOf)}`
export const not = `nesmí vyhovět schématu v "not"`
export const oneOf = `musí vyhovět právě jednomu schématu v "oneOf"`
export const pattern = (params) => `musí vyhovět regulárnímu výrazu "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `musí obsahovat položku vyhovující regulárnímu výrazu "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `název položky není platný`
export const required = (params) => `musí obsahovat požadovanou položku ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `musí být ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `nesmí obsahovat víc než ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `prvků`,
      'few': `prvky`
    },
    `prvek`
  )}`
export const unevaluatedProperties = `nesmí mít nehodnocené vlastnosti`
export const uniqueItems = (params) => `nesmí obsahovat duplicitní prvky (prvky ## ${__formatVariable(params?.j)} a ${__formatVariable(params?.i)} jsou identické)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `tag "${__formatVariable(params?.tag)}" musí být řetězec`,
      'mapping': `hodnota značky "${__formatVariable(params?.tag)}" musí být v mapování`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `nemůže mít další položky`,
      'missing': `musí obsahovat požadovanou položku ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `musí vyhovět alespoň jednomu schématu v "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
