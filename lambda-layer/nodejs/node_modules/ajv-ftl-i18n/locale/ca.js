const __locales = ["ca"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `ha de passar la validació de la clau "${params?.keyword}"`
const comparisonLimit = (params) => `ha de ser ${params?.comparison} ${params?.limit}`
export const items = (params) => `no ha de tenir més de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `element`
    },
    `elements`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `no ha de tenir propietats addicionals`
export const anyOf = `ha de coincidir amb algun esquema definit a "anyOf"`
export const _const = `ha de ser igual a la constant`
export const contains = `ha de contenir un ítem vàlid`
export const dependencies = (params) => `ha de contenir la ${__select(
    params?.depsCount,
    {
      'one': `propietat`
    },
    `propietats`
  )} ${__formatVariable(params?.deps)} quan la propietat ${__formatVariable(params?.property)} és present`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `ha de ser igual a un dels valors predefinits`
export const falseSchema = `l’esquema és fals`
export const format = (params) => `ha de coincidir amb el format "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `ha de correspondre’s amb l’esquema "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `no ha de tenir més de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `ítem`
    },
    `ítems`
  )}`
export const maxLength = (params) => `no pot contenir més de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `caràcter`
    },
    `caràcters`
  )}`
export const maxProperties = (params) => `no pot contenir més de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `propietat`
    },
    `propietats`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `no ha de tenir menys de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `ítem`
    },
    `ítems`
  )}`
export const minLength = (params) => `no pot contenir menys de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `caràcter`
    },
    `caràcters`
  )}`
export const minProperties = (params) => `no pot contenir menys de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `propietat`
    },
    `propietats`
  )}`
export const multipleOf = (params) => `ha de ser múltiple de ${__formatVariable(params?.multipleOf)}`
export const not = `no ha de ser vàlid d’acord amb l’esquema definit a "not"`
export const oneOf = `ha de coincidir només amb un esquema definit a "oneOf"`
export const pattern = (params) => `ha de coincidir amb el patró "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `la propietat ha de coincidir amb el patró "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `la propietat no és vàlida`
export const required = (params) => `ha de tenir la propietat requerida ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `ha de ser del tipus ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `no ha de tenir més de ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `ítem`
    },
    `ítems`
  )}`
export const unevaluatedProperties = `no ha de tenir propietats no avaluades`
export const uniqueItems = (params) => `no ha de tenir ítems duplicats (els ítems ## ${__formatVariable(params?.j)} i ${__formatVariable(params?.i)} són idèntics)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `l'etiqueta "${__formatVariable(params?.tag)}" ha de ser string`,
      'mapping': `el valor de l'etiqueta "${__formatVariable(params?.tag)}" ha d'estar a l'assignació`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `no ha de tenir propietats addicionals`,
      'missing': `ha de tenir la propietat requerida ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `ha de coincidir amb algun esquema definit a "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
