const __locales = ["th"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `ต้องผ่านคีย์เวิร์ด "${params?.keyword}"`
const comparisonLimit = (params) => `ต้อง ${params?.comparison} ${params?.limit}`
export const items = (params) => `ต้องมีสมาชิกไม่เกิน ${__formatVariable(params?.limit)} ตัว`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `ต้องไม่มี property อื่นๆ นอกเหนีอจากที่กำหนดไว้`
export const anyOf = `ต้องตรงกับหนึ่งใน schema ที่กำหนดไว้ใน "anyOf"`
export const _const = `ต้องเท่ากับค่าคงที่`
export const contains = `ต้องมีสมาชิกที่ผ่านเงื่อนไขอยู่`
export const dependencies = (params) => `เมื่อมี property ${__formatVariable(params?.property)} แล้วจะต้องมี property ${__formatVariable(params?.deps)} ด้วย`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `ต้องตรงกับหนึ่งในค่าที่กำหนดไว้`
export const falseSchema = `schema เป็น false`
export const format = (params) => `ต้องเป็นรูปแบบ "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `ต้องตรงกับ schema "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `ต้องมีสมาชิกไม่เกิน ${__formatVariable(params?.limit)}`
export const maxLength = (params) => `ต้องยาวไม่เกิน ${__formatVariable(params?.limit)} ตัวอักษร`
export const maxProperties = (params) => `ต้องมี property ไม่เกิน ${__formatVariable(params?.limit)} ตัว`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `ควรมีสมาชิกไม่น้อยกว่า ${__formatVariable(params?.limit)}`
export const minLength = (params) => `ต้องมีอย่างน้อย ${__formatVariable(params?.limit)} ตัวอักษร`
export const minProperties = (params) => `ต้องมี property อย่างน้อย ${__formatVariable(params?.limit)} ตัว`
export const multipleOf = (params) => `ต้องเป็นเลขที่หาร ${__formatVariable(params?.multipleOf)} ลงตัว`
export const not = `ต้องไม่ผ่าน schema ที่กำหนดไว้ใน "not"`
export const oneOf = `ต้องตรงกับ schema ตัวเดียวใน "oneOf" เท่านั้น`
export const pattern = (params) => `ต้องตรงตาม pattern "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `ต้องมี property ที่มีชื่อตรงตาม pattern "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `ชื่อ property ไม่ถูกต้อง`
export const required = (params) => `ต้องมี property ${__formatVariable(params?.missingProperty)} ด้วย`
export const type = (params) => `ต้องเป็น ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `ต้องมีไม่เกิน ${__formatVariable(params?.len)} ตัว`
export const unevaluatedProperties = `ต้องไม่มี property ที่ยังไม่ได้ผ่านการตรวจสอบเงื่อนไขใดๆ`
export const uniqueItems = (params) => `ต้องมีสมาชิกไม่ซ้ำักัน (ลำดับที่ ${__formatVariable(params?.j)} กับ ${__formatVariable(params?.i)} ซ้ำกัน)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `tag "${__formatVariable(params?.tag)}" ต้องเป็น string`,
      'mapping': `ต้องมีค่าของ tag "${__formatVariable(params?.tag)}" ใน mapping`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `ต้องไม่มี property อื่นๆ นอกเหนีอจากที่กำหนดไว้`,
      'missing': `ต้องมี property ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `ต้องตรงกับหนึ่งใน schema ที่กำหนดไว้ใน "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
