const __locales = ["sv"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `bör passera "${params?.keyword}" nyckelord validering`
const comparisonLimitFormat = (params) => `bör vara ${params?.comparison} ${params?.limit}`
const comparisonLimit = (params) => `borde vara ${params?.comparison} ${params?.limit}`
export const items = (params) => `borde ha fler än ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `sak`
    },
    `saker`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `borde inte ha fler egenskaper`
export const anyOf = `borde matcha något schema i "anyOf"`
export const _const = `bör vara en konstant`
export const contains = `bör innehålla ett giltigt objekt`
export const dependencies = (params) => `borde ha ${__select(
    params?.depsCount,
    {
      'one': `egenskap`
    },
    `egenskaper`
  )} ${__formatVariable(params?.deps)} när egenskap ${__formatVariable(params?.property)} finns tillgängligt`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `borde vara ekvivalent med en av dess fördefinierade värden`
export const falseSchema = `boolean schema är falskt`
export const format = (params) => `borde matcha formatet "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimitFormat(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimitFormat(params)}`
export const formatMinimum = (params) => `${comparisonLimitFormat(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimitFormat(params)}`
export const _if = (params) => `måste matcha schemat "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `borde inte ha fler än ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `sak`
    },
    `saker`
  )}`
export const maxLength = (params) => `borde inte vara längre än ${__formatVariable(params?.limit)} tecken`
export const maxProperties = (params) => `borde inte ha fler än ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `egenskap`
    },
    `egenskaper`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `borde inte ha färre än ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `sak`
    },
    `saker`
  )}`
export const minLength = (params) => `borde inte vara kortare än ${__formatVariable(params?.limit)} tecken`
export const minProperties = (params) => `borde inte ha färre än ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `egenskap`
    },
    `egenskaper`
  )}`
export const multipleOf = (params) => `borde vara en multipel av ${__formatVariable(params?.multipleOf)}`
export const not = `borde inte vara giltigt enligt schema i "not"`
export const oneOf = `borde matcha exakt ett schema i "oneOf"`
export const pattern = (params) => `borde matcha mönstret "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `bör ha en egenskap som matchar mönstret "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `egenskap med namnet är inte giltig`
export const required = (params) => `borde ha den nödvändiga egenskapen ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `borde vara ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `borde inte ha fler än ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `sak`
    },
    `saker`
  )}`
export const unevaluatedProperties = `får inte ha obedömda egenskaper`
export const uniqueItems = (params) => `borde inte ha duplicerade saker (sakerna ## ${__formatVariable(params?.j)} och ${__formatVariable(params?.i)} är identiska)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `taggen "${__formatVariable(params?.tag)}" måste vara sträng`,
      'mapping': `värdet för taggen "${__formatVariable(params?.tag)}" måste vara i mappningen`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `borde inte ha fler egenskaper`,
      'missing': `borde ha den nödvändiga egenskapen ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `borde matcha något schema i "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
