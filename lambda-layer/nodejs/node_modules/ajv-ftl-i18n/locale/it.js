const __locales = ["it"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `deve essere valido secondo il criterio "${params?.keyword}"`
const comparisonLimit = (params) => `deve essere ${params?.comparison} ${params?.limit}`
export const items = (params) => `non dovrebbe avere più di ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `elemento`
    },
    `elementi`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `non deve avere attributi aggiuntivi`
export const anyOf = `deve corrispondere ad uno degli schema in "anyOf"`
export const _const = `deve essere uguale alla costante`
export const contains = `deve contentere un elemento valido`
export const dependencies = (params) => `dovrebbe avere ${__select(
    params?.depsCount,
    {
      'one': `l'attributo`
    },
    `gliattributi`
  )} ${__formatVariable(params?.deps)} quando l'attributo ${__formatVariable(params?.property)} è presente`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `deve essere uguale ad uno dei valori consentiti`
export const falseSchema = `lo schema booleano è falso`
export const format = (params) => `deve corrispondere al formato "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `deve corrispondere allo schema "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `non deve avere più di ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `elemento`
    },
    `elementi`
  )}`
export const maxLength = (params) => `non deve essere più lungo di ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `carattere`
    },
    `caratteri`
  )}`
export const maxProperties = (params) => `non deve avere più di ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `attributo`
    },
    `attributi`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `non deve avere meno di ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `elemento`
    },
    `elementi`
  )}`
export const minLength = (params) => `non deve essere meno lungo di ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `carattere`
    },
    `caratteri`
  )}`
export const minProperties = (params) => `non deve avere meno di ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `attributo`
    },
    `attributi`
  )}`
export const multipleOf = (params) => `deve essere un multiplo di ${__formatVariable(params?.multipleOf)}`
export const not = `non deve essere valido in base allo schema di "not"`
export const oneOf = `deve corrispondere esattamente ad uno degli schema in "oneOf"`
export const pattern = (params) => `deve corrispondere al formato "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `deve avere un attributo che corrisponda al formato "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `il nome dell'attritbuto non è valido`
export const required = (params) => `deve avere l'attributo obbligatorio ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `deve essere di tipo ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `non deve avere più di ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `elementio`
    },
    `elementii`
  )}`
export const unevaluatedProperties = `non deve avere attributi non valutati`
export const uniqueItems = (params) => `non deve avere duplicati (gli elementi ## ${__formatVariable(params?.j)} e ${__formatVariable(params?.i)} sono uguali)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `il tag "${__formatVariable(params?.tag)}" deve essere di tipo stringa`,
      'mapping': `il valore del tag "${__formatVariable(params?.tag)}" deve essere nei mapping`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `non deve avere attributi addizionali`,
      'missing': `deve avere l'attributo obbligatorio ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `deve corrispondere ad uno schema in "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
