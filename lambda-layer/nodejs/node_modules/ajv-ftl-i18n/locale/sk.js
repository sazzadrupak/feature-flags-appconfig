const __locales = ["sk"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `musí splniť "${params?.keyword}" validáciu`
const comparisonLimit = (params) => `musí byť ${params?.comparison} ${params?.limit}`
export const items = (params) => `nemôže obsahovať viac, než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `prvok`
    },
    `prvkov`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `nemôže obsahovať ďalšie položky`
export const anyOf = `musí splňovať aspoň jednu zo schém v "anyOf"`
export const _const = `musí byť konštanta`
export const contains = `musí obsahovať prvok zodpovedajúci schéme`
export const dependencies = (params) => `musí obsahovať ${__select(
    params?.depsCount,
    {
      'one': `položka`,
      'few': `položky`
    },
    `položiek`
  )}: ${__formatVariable(params?.deps)}, ak obsahuje ${__formatVariable(params?.property)}`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `musí byť jedna z definovaných hodnôt`
export const falseSchema = `schéma je false`
export const format = (params) => `musí obsahovať formát "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `musí sa zhodovať so schémou "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `nesmie obsahovať viac než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `prvok`
    },
    `prvkov`
  )}`
export const maxLength = (params) => `nesmie byť dlhší než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `znak`
    },
    `znakov`
  )}`
export const maxProperties = (params) => `nesmie obsahovať viac než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `položka`,
      'few': `položky`
    },
    `položiek`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `nesmie obsahovať menej než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `prvok`
    },
    `prvkov`
  )}`
export const minLength = (params) => `nesmie byť kratší než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `znak`
    },
    `znakov`
  )}`
export const minProperties = (params) => `nesmie obsahovať menej než ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `položka`,
      'few': `položky`
    },
    `položiek`
  )}`
export const multipleOf = (params) => `musí byť násobkom ${__formatVariable(params?.multipleOf)}`
export const not = `nesmie splňovať schému v "not"`
export const oneOf = `musí splňovať práve jednu schému v "oneOf"`
export const pattern = (params) => `musí splňovať regulárny výraz "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `musí obsahovať položku splňjúcu regulárny výraz "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `názov položky nezodpovedá schéme`
export const required = (params) => `musí obsahovať požadovanú položku ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `musí byť ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `nesmie obsahovať viac než ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `prvok`
    },
    `prvkov`
  )}`
export const unevaluatedProperties = `nesmú mať neohodnotené vlastnosti`
export const uniqueItems = (params) => `nesmie obsahovať duplicitné prvky (prvky ## ${__formatVariable(params?.j)} a ${__formatVariable(params?.i)} sú rovnaké)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `značka "${__formatVariable(params?.tag)}" musí byť reťazec`,
      'mapping': `hodnota značky "${__formatVariable(params?.tag)}" musí byť v mapovaní`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `nemôže obsahovať ďalšie položky`,
      'missing': `musí obsahovať požadovanú položku ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `musí splňovať aspoň jednu zo schém v "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
