const __locales = ["de"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `muss die Validierung "${params?.keyword}" bestehen`
const comparisonLimit = (params) => `muss ${params?.comparison} ${params?.limit} sein`
export const items = (params) => `darf nicht mehr als ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `Element`
    },
    `Elemente`
  )} enthalten`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `darf keine zusätzlichen Attribute haben`
export const anyOf = `muss einem der Schemata in "anyOf" entsprechen`
export const _const = `muss gleich der Konstanten sein`
export const contains = `muss ein valides Element enthalten`
export const dependencies = (params) => `muss ${__select(
    params?.depsCount,
    {
      'one': `Attribut`
    },
    `Attribute`
  )} ${__formatVariable(params?.deps)} aufweisen, wenn Attribut ${__formatVariable(params?.property)} gesetzt ist`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `muss einem der vorgegebenen Werte entsprechen`
export const falseSchema = `boolesches Schema ist falsch`
export const format = (params) => `muss diesem Format entsprechen: "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `muss dem Schema "${__formatVariable(params?.failingKeyword)}" entsprechen`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `darf nicht mehr als ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `Element`
    },
    `Elemente`
  )} haben`
export const maxLength = (params) => `darf nicht länger als ${__formatVariable(params?.limit)} Zeichen sein`
export const maxProperties = (params) => `darf nicht mehr als ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `Attribut`
    },
    `Attribute`
  )} haben`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `darf nicht weniger als ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `Element`
    },
    `Elemente`
  )} haben`
export const minLength = (params) => `darf nicht kürzer als ${__formatVariable(params?.limit)} Zeichen sein`
export const minProperties = (params) => `darf nicht weniger als ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `Attribut`
    },
    `Attribute`
  )} haben`
export const multipleOf = (params) => `muss ein Vielfaches von ${__formatVariable(params?.multipleOf)} sein`
export const not = `muss dem in "not" angegebenen Schema widersprechen`
export const oneOf = `muss genau einem der Schemata in "oneOf" entsprechen`
export const pattern = (params) => `muss diesem Muster entsprechen: "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `muss ein Attribut nach folgendem Muster haben "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `Attributname ist ungültig`
export const required = (params) => `muss das erforderliche Attribut ${__formatVariable(params?.missingProperty)} enthalten`
export const type = (params) => `muss sein: ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `darf nicht mehr als ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `Element`
    },
    `Elemente`
  )} haben`
export const unevaluatedProperties = `darf keine unausgewerteten Attribute haben`
export const uniqueItems = (params) => `darf keine Duplikate enthalten (Elemente #${__formatVariable(params?.j)} und #${__formatVariable(params?.i)} sind gleich)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `der Tag "${__formatVariable(params?.tag)}" muss eine Zeichenkette sein`,
      'mapping': `der Wert vom Tag "${__formatVariable(params?.tag)}" muss im oneOf enthalten sein`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `darf keine zusätzlichen Attribute haben`,
      'missing': `muss das erforderliche Attribut ${__formatVariable(params?.missingProperty)} enthalten`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `muss einem der Schemata in "union" entsprechen`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
