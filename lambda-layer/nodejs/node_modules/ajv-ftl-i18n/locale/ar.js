const __locales = ["ar"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `يجب أن تمرر كلمة التحقق المفتاحية "${params?.keyword}"`
const comparisonLimit = (params) => `يجب أن يكون ${params?.comparison} ${params?.limit}`
export const items = (params) => `يجب أن لا يحوي أكثر من ${__formatVariable(params?.limit)} عنصر`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `يجب أن لا يحوي خصائص إضافية`
export const anyOf = `يجب أن يوافق أحد المخططات الموجودة في "anyOf"`
export const _const = `يجب أن يكون ثابتاً`
export const contains = `يجب أن يحوي عنصرا صحيح`
export const dependencies = (params) => `يجب أن يحوي الخصائص ${__formatVariable(params?.deps)} عندما تكون الخاصية ${__formatVariable(params?.property)} موجودة`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `قيمة هذا الحقل يجب أن تكون مساوية لأحد القيم المعرفة مسبقاً`
export const falseSchema = `المخطط المنطقي غير صحيح`
export const format = (params) => `يجب أن يوافق الصيغة "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `يجب أن توافق المخطط "${__formatVariable(params?.failingKeyword)}"`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `يجب أن لا يحوي أكثر من ${__formatVariable(params?.limit)} عنصر`
export const maxLength = (params) => `يجب أن لا يحوي أكثر من ${__formatVariable(params?.limit)} محرف`
export const maxProperties = (params) => `يجب أن لا يحوي أكثر من ${__formatVariable(params?.limit)} خصائص`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `يجب أن لا يحوي أقل من ${__formatVariable(params?.limit)} عنصر`
export const minLength = (params) => `يجب أن لا يحوي أقل من ${__formatVariable(params?.limit)} محرف`
export const minProperties = (params) => `يجب أن لا يحوي أقل من ${__formatVariable(params?.limit)} خصائص`
export const multipleOf = (params) => `يجب أن يحوي أكثر من ${__formatVariable(params?.multipleOf)}`
export const not = `يجب أن يكون غير صحيح وفقاً للمخطط "not"`
export const oneOf = `يجب أن يوافق مخطط واحد فقط موجود في "oneOf"`
export const pattern = (params) => `يجب أن يوافق النمط "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `يجب أن يحوي خاصية توافق النمط "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `اسم الخاصية غير صالح`
export const required = `هذا الحقل إلزامي`
export const type = `قيمة هذا الحقل غير صالحة`
export const unevaluatedItems = (params) => `يجب أن لا يحوي أكثر من ${__formatVariable(params?.len)} عنصر`
export const unevaluatedProperties = `يجب ألا يكون لديك خصائص غير مقيمة`
export const uniqueItems = (params) => `يجب أن لا يحوي عناصر مكررة (العنصر ## ${__formatVariable(params?.j)} و ${__formatVariable(params?.i)} متطابقة)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `العلامة "${__formatVariable(params?.tag)}" يجب أن تكون سلسلة`,
      'mapping': `يجب أن تكون قيمة العلامة "${__formatVariable(params?.tag)}" في التعيين`
    },
    `${type}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `يجب ألا يكون لديك خصائص إضافية`,
      'missing': `يجب أن يكون لديك خاصية ${__formatVariable(params?.missingProperty)}`
    },
    `${type}`
  )}`
export const elements = `${type}`
export const values = `${type}`
export const union = `يجب أن يتطابق مع مخطط في "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
