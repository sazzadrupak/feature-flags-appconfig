const __locales = ["fr"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `doit être valide selon le critère "${params?.keyword}"`
const comparisonLimit = (params) => `doit être ${params?.comparison} ${params?.limit}`
export const items = (params) => `ne doit pas contenir plus de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `élémént`
    },
    `éléménts`
  )}`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `ne doit pas contenir de propriétés additionnelles`
export const anyOf = `doit correspondre à un schéma de « anyOf »`
export const _const = `doit être égal à la constante`
export const contains = `doit contenir un élément valide`
export const dependencies = (params) => `doit avoir la propriété ${__formatVariable(params?.deps)} quand la propriété ${__formatVariable(params?.property)} est présente`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `doit être égal à une des valeurs prédéfinies`
export const falseSchema = `le schema est « false »`
export const format = (params) => `doit correspondre au format « ${__formatVariable(params?.format)} »`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `doit correspondre au schéma « ${__formatVariable(params?.failingKeyword)} »`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const maxItems = (params) => `ne doit pas contenir plus de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `élément`
    },
    `éléments`
  )}`
export const maxLength = (params) => `ne doit pas dépasser ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `caractère`
    },
    `caractères`
  )}`
export const maxProperties = (params) => `ne doit pas contenir plus de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `propriété`
    },
    `propriétés`
  )}`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const minItems = (params) => `ne doit pas contenir moins de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `élément`
    },
    `éléments`
  )}`
export const minLength = (params) => `ne doit pas faire moins de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `caractère`
    },
    `caractères`
  )}`
export const minProperties = (params) => `ne doit pas contenir moins de ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `propriété`
    },
    `propriétés`
  )}`
export const multipleOf = (params) => `doit être un multiple de ${__formatVariable(params?.multipleOf)}`
export const not = `est invalide selon le schéma « not »`
export const oneOf = `doit correspondre à exactement un schéma de « oneOf »`
export const pattern = (params) => `doit correspondre au format « ${__formatVariable(params?.pattern)} »`
export const patternRequired = (params) => `la propriété doit correspondre au format « ${__formatVariable(params?.missingPattern)} »`
export const propertyNames = `le nom de propriété est invalide`
export const required = (params) => `requiert la propriété ${__formatVariable(params?.missingProperty)}`
export const type = (params) => `doit être de type ${__formatVariable(params?.type)}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )}`
export const unevaluatedItems = (params) => `ne doit pas contenir plus de ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `élément`
    },
    `éléments`
  )}`
export const unevaluatedProperties = `ne doit pas avoir de propriétés non évaluées`
export const uniqueItems = (params) => `ne doit pas contenir de doublons (les éléments ## ${__formatVariable(params?.j)} et ${__formatVariable(params?.i)} sont identiques)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `la balise « ${__formatVariable(params?.tag)} » doit être une chaîne`,
      'mapping': `la valeur de la balise « ${__formatVariable(params?.tag)} » doit être dans le mappage`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `ne doit pas contenir de propriétés additionnelles`,
      'missing': `requiert la propriété ${__formatVariable(params?.missingProperty)}`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `doit correspondre à un schéma de « union »`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
