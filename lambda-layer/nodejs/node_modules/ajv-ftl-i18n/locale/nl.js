const __locales = ["nl"]

const __formatNumber = (value, options) => {
	return new Intl.NumberFormat(__locales, options).format(value)
}

const __formatVariable = (value) => {
  if (typeof value === 'string') return value
  const decimal =  Number.parseFloat(value)
  const number = Number.isInteger(decimal) ? Number.parseInt(value) : decimal
  return __formatNumber(number)
}

const __select = (value, cases, fallback, options) => {
	const pluralRules = new Intl.PluralRules(__locales, options)
	const rule = pluralRules.select(value)
	return cases[value] ?? cases[rule] ?? fallback
}

const defaultMessage = (params) => `moet sleutelwoord validatie "${params?.keyword}" doorstaan`
const comparisonLimit = (params) => `moet ${params?.comparison} ${params?.limit} zijn`
export const items = (params) => `mag niet meer dan ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `item`
    },
    `items`
  )} bevatten`
export const additionalItems = (params) => `${items(params)}`
export const additionalProperties = `mag geen extra eigenschappen bevatten`
export const anyOf = `moet overeenkomen met een schema in "anyOf"`
export const _const = `moet gelijk zijn aan constante`
export const contains = `moet een geldig item bevatten`
export const dependencies = (params) => `moet de ${__select(
    params?.depsCount,
    {
      'one': `eigenschap`
    },
    `eigenschappen`
  )} ${__formatVariable(params?.deps)} bevatten als ${__formatVariable(params?.property)} is gedefinieerd`
export const dependentRequired = (params) => `${dependencies(params)}`
export const _enum = `moet overeenkomen met één van de voorgedefinieerde waarden`
export const falseSchema = `boolean schema is fout`
export const format = (params) => `moet overeenkomen met het volgende formaat: "${__formatVariable(params?.format)}"`
export const formatMaximum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMaximum = (params) => `${comparisonLimit(params)}`
export const formatMinimum = (params) => `${comparisonLimit(params)}`
export const formatExclusiveMinimum = (params) => `${comparisonLimit(params)}`
export const _if = (params) => `moet overeenkomen met "${__formatVariable(params?.failingKeyword)}" schema`
export const maximum = (params) => `${comparisonLimit(params)}`
export const exclusiveMaximum = (params) => `${maximum(params)}`
export const maxItems = (params) => `mag niet meer dan ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `item`
    },
    `items`
  )} bevatten`
export const maxLength = (params) => `mag niet langer dan ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `karakter`
    },
    `karakters`
  )} zijn`
export const maxProperties = (params) => `mag niet meer dan ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `eigenschap`
    },
    `eigenschappen`
  )} bevatten`
export const minimum = (params) => `${comparisonLimit(params)}`
export const exclusiveMinimum = (params) => `${minimum(params)}`
export const minItems = (params) => `mag niet minder dan ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `item`
    },
    `items`
  )} bevatten`
export const minLength = (params) => `mag niet korter dan ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `karakter`
    },
    `karakters`
  )} zijn`
export const minProperties = (params) => `mag niet minder dan ${__formatVariable(params?.limit)} ${__select(
    params?.limit,
    {
      'one': `eigenschap`
    },
    `eigenschappen`
  )} bevatten`
export const multipleOf = (params) => `moet een veelvoud van ${__formatVariable(params?.multipleOf)} zijn`
export const not = `mag niet overeenkomen met een schema in "not"`
export const oneOf = `moet overeenkomen met één schema in "oneOf"`
export const pattern = (params) => `moet overeenkomen met het volgende patroon: "${__formatVariable(params?.pattern)}"`
export const patternRequired = (params) => `moet een eigenschap bevatten die overeenkomt met het pattroon: "${__formatVariable(params?.missingPattern)}"`
export const propertyNames = `eigenschapnaam is ongeldig`
export const required = (params) => `moet de eigenschap ${__formatVariable(params?.missingProperty)} bevatten`
export const type = (params) => `moet een ${__select(
    params?.type,
    {
      'number': `nummer`,
      'integer': `geheel getal`,
      'string': `tekenreeks`,
      'boolean': `ja of nee waarde`
    },
    `${__formatVariable(params?.type)}`
  )}${__select(
    params?.nullable,
    {
      'true': `/null`
    },
    `${""}`
  )} bevatten`
export const unevaluatedItems = (params) => `mag niet meer dan ${__formatVariable(params?.len)} ${__select(
    params?.len,
    {
      'one': `item`
    },
    `items`
  )} bevatten`
export const unevaluatedProperties = `mag geen ongecontroleerde eigenschappen bevatten`
export const uniqueItems = (params) => `mag geen gedupliceerde items bevatten (items ## ${__formatVariable(params?.j)} en ${__formatVariable(params?.i)} zijn identiek)`
export const discriminator = (params) => `${__select(
    params?.error,
    {
      'tag': `tag "${__formatVariable(params?.tag)}" moet een tekenreeks zijn`,
      'mapping': `de waarde van het veld "${__formatVariable(params?.tag)}" moet voorkomen in de mapping`
    },
    `${type(params)}`
  )}`
export const properties = (params) => `${__select(
    params?.error,
    {
      'additional': `mag geen extra eigenschappen bevatten`,
      'missing': `moet de eigenschap ${__formatVariable(params?.missingProperty)} bevatten`
    },
    `${type(params)}`
  )}`
export const elements = (params) => `${type(params)}`
export const values = (params) => `${type(params)}`
export const union = `moet overeenkomen met een schema in "union"`
const __exports = {
  items,
  additionalItems,
  additionalProperties,
  anyOf,
  'const': _const,
  contains,
  dependencies,
  dependentRequired,
  'enum': _enum,
  falseSchema,
  format,
  formatMaximum,
  formatExclusiveMaximum,
  formatMinimum,
  formatExclusiveMinimum,
  'if': _if,
  maximum,
  exclusiveMaximum,
  maxItems,
  maxLength,
  maxProperties,
  minimum,
  exclusiveMinimum,
  minItems,
  minLength,
  minProperties,
  multipleOf,
  not,
  oneOf,
  pattern,
  patternRequired,
  propertyNames,
  required,
  type,
  unevaluatedItems,
  unevaluatedProperties,
  uniqueItems,
  discriminator,
  properties,
  elements,
  values,
  union
}
export default (errors) => {
  if (!errors?.length) return
  for (const e of errors) {
    let { keyword } = e
    if (keyword === 'false schema') keyword = 'falseSchema'
    let source = __exports[keyword] ?? __exports['_'+keyword]
    let values = {}
    if (keyword === 'errorMessage') {
      const [message, ...valuesPairs] = e.message.split(', ')
      ;(valuesPairs.join(', ').match(regExpJsonPointerPairs) ?? []).forEach(pair => {
        const [key, value] = pair.split(':')
        values[key] = value.replace(regExpJsonPointerQuote, '')
      })
      source ??= __exports[message] ?? __exports['_'+message] ?? e.message
    } else {
      source ??= __exports.defaultMessage
    }
    if (typeof source === 'function') {
      e.message = source({keyword:e.keyword, ...e.params, ...values})
    } else {
      e.message = source
    }
  }
}
const regExpJsonPointerPairs = /([a-zA-Z0-9_-]+):(".*"|[^,"]+)/g
const regExpJsonPointerQuote = /(^"|"$)/g
