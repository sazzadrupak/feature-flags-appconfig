# Layer Version Configuration
# Update these values when deploying new layer versions

# AWS Account ID (replace with your actual account ID)
AWS_ACCOUNT_ID=YOUR_ACCOUNT_ID

# AWS Region
AWS_REGION=us-east-1

# Layer Versions per Environment
# Format: environment:layer_name:version_number

# Development Environment - Uses latest layer for development
DEV_LAYER_ARN=arn:aws:lambda:${AWS_REGION}:${AWS_ACCOUNT_ID}:layer:feature-flags-deps-dev:1

# Staging Environment - Uses stable tested layer version
STAGING_LAYER_ARN=arn:aws:lambda:${AWS_REGION}:${AWS_ACCOUNT_ID}:layer:feature-flags-deps-staging:1

# Production Environment - Uses well-tested stable layer version
PROD_LAYER_ARN=arn:aws:lambda:${AWS_REGION}:${AWS_REGION}:layer:feature-flags-deps-prod:1

# Layer Version History (for reference)
# v1.0.0 - Initial layer with basic dependencies
# v1.1.0 - Added new powertools features
# v1.2.0 - Updated AWS SDK to latest version